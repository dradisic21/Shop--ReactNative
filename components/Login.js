import {
    Button,
    StyleSheet,
    Text,
    View,
    Image,
  } from "react-native";

  export default function Login() {
    return (
      <View>
          
      </View>
    );
  }

  const styles = StyleSheet.create({
    container: {
      backgroundColor: "#fff",
      alignItems: "center",
      justifyContent: "center",
      position: "relative",
    },
  });
  